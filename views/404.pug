doctype html
html
  head
    title 404
    meta(charset='utf8')
    style(ref='stylesheet').
      .text-center{
        text-align: center;
      }
  body
    h1.text-center 404
    h1.text-center 您要访问的页面不存在

    .text-center
      span#time 3
      |秒后将返回首页
  
    script.
      let t = 3
      const fn = () => {
        time.innerHTML = --t
        if(t === 0) location.href = "/"
      }
      setInterval(fn , 1000)